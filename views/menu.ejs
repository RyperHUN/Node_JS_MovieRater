<nav class="navbar navbar-expand-md navbar-dark bg-dark fixed-top">
    <a class="navbar-brand" href="#">IMRB Movies</a>
    <button class="navbar-toggler collapsed" type="button" data-toggle="collapse" data-target="#navbarsExampleDefault" aria-controls="navbarsExampleDefault" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

  <% var makeMenuItem = function(name,menuName,isLoggedIn) { %>
    <%if((!isLoggedIn) || (typeof user !== 'undefined')) {%>
      <li
        <% if(viewName == name) { %>
          class="nav item active"
        <% } else { %>
          class="nav-item"
          <% } %>
      >
        <a class="nav-link" href=<%= name -%>> 
          <%= menuName -%>
          <span class="sr-only">(current)</span>
        </a>
      </li>
    <%} %>
  <%}%>
    <div class="navbar-collapse collapse" id="navbarsExampleDefault">
      <ul class="navbar-nav mr-auto">
        <% makeMenuItem("/","Toplist", false); %>
        <!-- LOGIN is only visible if not logged in -->
        <%if((typeof user === 'undefined')) {%>
          <% makeMenuItem("/login","Log-in", false); %>
        <% } %>
        <% makeMenuItem("/profile","Profile", true); %>
        <% makeMenuItem("/logout","Log Out", true); %>
      <!-- TODO LOGOUT ROUTE -->
      </ul>
      <form class="form-inline my-2 my-lg-0" action="/search" method="GET">
        <input class="form-control mr-sm-2" placeholder="Search" aria-label="Search" type="text" name="movieName">
        <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
      </form>
    </div>
</nav>
